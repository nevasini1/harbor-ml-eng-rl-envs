# Verifier calibration

<!-- generated by common/calibration.py; edit the sources, not this -->

What each verifier scored on inputs of known quality, and whether the
ordering came out as expected. Required by the assignment's §4:

> Run the verifier on at least: the untouched base model, the reference
> counterpart, your probe fine-tune, a deliberately broken checkpoint, and
> (if feasible) a deliberate reward-hacking attempt. **Report the reward each
> produced and confirm the ordering matches expectation.** A verifier that has
> only ever seen "good" inputs is untested.

Assembled from the committed records rather than retyped, so it cannot drift
from them. Regenerate with `python common/calibration.py`; `--check` fails if
this file is stale **or if an ordering test fails**. Wired into CI.

## Ordering

| task | ordering as expected | |
|---|---|---|
| `mol-property-adapt` | **yes** | broken inputs all 0; every no-adaptation input below the oracle at 0.909654 |
| `pref-reward-model` | **yes** | broken inputs all 0; every no-adaptation input below the oracle at 0.582804 |
| `qa-sft-adapt` | **yes** | broken inputs all 0; every no-adaptation input below the oracle at 1.0 |
| `sciml-protein-regression` | **NO** (acknowledged) | a submission that does not adapt the model scores at or above the oracle: frozen probe (no encoder adaptation) = 1.0 vs oracle 1.0 |

The protein failure is the reason this table is worth assembling: its frozen
probe and its oracle both score 1.0, because both fixed tiers sit below the
frozen ceiling of 0.5332. No individual source shows that -- only the rows
side by side do. See [decision 0007](decisions/0007-protein-scores-three-fixed-tiers.md).

## `mol-property-adapt`

| input | reward | verdict | note |
|---|---|---|---|
| oracle (shipped solution/) | 0.909654 | accepted | regrade through the refactored grader; the recorded original was 0.909661 |
| live agent (codex, k=1) | 1 | accepted | both eval sets clipped at recovery 1.0; raw 1.280 and 1.472 |
| broken/hostile input: corrupt | 0 | error | expected error\|rejected |
| broken/hostile input: empty | 0 | rejected | expected rejected |
| broken/hostile input: hostile-config | 0 | rejected | expected rejected |
| broken/hostile input: no-config | 0 | rejected | expected rejected |
| broken/hostile input: shuffled | 0 | rejected | expected rejected |
| broken/hostile input: swap-35M | 0 | rejected | expected rejected |
| broken/hostile input: symlink | 0 | rejected | expected rejected |
| no-adaptation input: base | 0 | ok | expected ok |
| no-adaptation input: constant | 0 | ok | expected ok |

Sources: `jobs/`, `research/results/crash_matrix.json`

## `pref-reward-model`

| input | reward | verdict | note |
|---|---|---|---|
| oracle (shipped solution/) | 0.582804 | accepted | 1.3 sigma under a reference that is a five-seed mean |
| live agent (codex, k=1) | 0.864497 | accepted | scored 0.0 at the time by the false reject in decision 0009; 0.865 after the fix |
| broken/hostile input: contaminated | 0 | rejected | agent log quoting held-out rows; shingle overlap  (transcribed, not re-measured) |
| broken/hostile input: nan | 0 | rejected | non-finite weights in embeddings.word_embeddings.weight  (transcribed, not re-measured) |
| broken/hostile input: public_twin | 0 | rejected | bit-identical all-distilroberta-v1; caught by sha256, the only layer that sees it  (transcribed, not re-measured) |
| broken/hostile input: shuffled | 0 | rejected | min per-tensor cosine 0.0071 < 0.9  (transcribed, not re-measured) |
| broken/hostile input: truncated | 0 | rejected | missing config.json  (transcribed, not re-measured) |
| no-adaptation input: base_unchanged | 0 | accepted | provided base, fresh head  (transcribed, not re-measured) |

Sources: `jobs/`, `research/posttrain/results/verifier_fixtures.json`

## `qa-sft-adapt`

| input | reward | verdict | note |
|---|---|---|---|
| oracle (shipped solution/) | 1 | accepted |  |
| live agent (codex, k=1) | 0.734115 | accepted | the only task that neither clipped to 1.0 nor floored to 0 |
| broken/hostile input: contaminated | 0 | rejected | agent log quoting held-out rows; shingle overlap  (transcribed, not re-measured) |
| broken/hostile input: laundered | 0 | rejected | SmolLM2-135M-Instruct + 1e-4 noise; caught by nearest-ancestor, the only layer that sees it  (transcribed, not re-measured) |
| broken/hostile input: nan | 0 | rejected | non-finite weights in embed_tokens.weight  (transcribed, not re-measured) |
| broken/hostile input: shuffled | 0 | rejected | min per-tensor cosine 0.0002 < 0.9  (transcribed, not re-measured) |
| broken/hostile input: truncated | 0 | rejected | missing config.json  (transcribed, not re-measured) |
| no-adaptation input: base_unchanged | 0 | accepted | provided base with a fresh head; raw recovery -0.014 / -0.364 / 0.0 recorded, not hidden  (transcribed, not re-measured) |

Sources: `jobs/`, `research/posttrain/results/verifier_fixtures.json`

## `sciml-protein-regression`

| input | reward | verdict | note |
|---|---|---|---|
| oracle (shipped solution/) | 1 | accepted | spearman 0.5733 |
| frozen probe (no encoder adaptation) | 1 | accepted | spearman 0.5358 -- SCORES THE SAME AS THE ORACLE, see decision 0007 |

Sources: `jobs/`

## What is not here

- **The reference counterpart is not run as a submission on any task.** The
  oracle stands in for it. On `mol-property-adapt` that substitution is known
  to be unreliable: the oracle scores 0.6896 against a `reference_auc` of
  0.7019, which is open question 1.
- **`sciml-protein-regression` has no broken-input row here.** Its hardening
  suite (`scripts/modal_verify_hardening.py`) runs on Modal and its committed
  record covers the mol grader, not this one.
- The two post-training tracks' fixture rows are transcribed from the recorded
  run until `verify_graders.py` is next executed, which now writes them as data.
